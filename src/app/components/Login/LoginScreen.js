import React from 'react';
import { View , Text } from 'react-native';
import { SharedStyle} from '../../styles/SharedStyle';

export function LoginScreen() {
    return (
        <View style={SharedStyle.container}>
            <Text> Add LoginScreen! </Text>
        </View>
    )
}