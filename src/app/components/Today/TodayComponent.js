import React from 'react';
import { View , Text } from 'react-native';
import { SharedStyle} from '../../styles/SharedStyle';

export function TodayScreen() {
    return (
        <View style={SharedStyle.container}>
            <Text> Add TodayScreen! </Text>
        </View>
    )
}